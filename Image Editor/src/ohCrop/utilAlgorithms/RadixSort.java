package ohCrop.utilAlgorithms;

import org.jocl.cl_command_queue;
import org.jocl.cl_context;
import org.jocl.cl_device_id;

/**
 * Sorts elements using Radix Sort.
 * @author Sean Maloney
 */
public class RadixSort {
	
	/**
	 * Sorts elements non comparatively based on bit values.
	 * @param data The data to be sorted. 
	 */
	public static void sort(final int[] data, int[] results, cl_context context, cl_command_queue commandQueue, cl_device_id device, String kernelMethod) {
		
		
		
	}
	
	
}
